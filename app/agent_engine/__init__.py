"""Agent engine — ReAct loop, Tool/Skill protocols, LLM adapter.

Implementation lives in 波次 1B (MUR-9). This package currently exposes
typing protocols only; concrete classes appear once the contract is
implemented. See docs/contracts/agent-interface.md for the contract.
"""
